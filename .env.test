# Test Environment Configuration
# This file is committed to version control for consistent testing

# Cloud Update Service Configuration
CLOUD_UPDATE_PORT=9999
CLOUD_UPDATE_SECRET=test-secret-key-for-e2e-testing-purposes-only
CLOUD_UPDATE_LOG_LEVEL=debug

# E2E Test Configuration
E2E_BASE_URL_PREFIX=http://localhost
E2E_SECRET=test-secret-key-for-e2e-testing-purposes-only
E2E_TIMEOUT=30

# Port mapping for each distribution
E2E_PORT_ALPINE=9991
E2E_PORT_UBUNTU=9992
E2E_PORT_DEBIAN=9993
E2E_PORT_ROCKYLINUX=9994
E2E_PORT_FEDORA=9995
E2E_PORT_ARCH=9996
E2E_PORT_OPENSUSE=9997

# Docker Configuration
DOCKER_COMPOSE_FILE=src/test/e2e/docker-compose.yml
DOCKER_BUILD_TIMEOUT=300
DOCKER_NETWORK=cloud-update-test

# Test Configuration
TEST_PAYLOAD='{"action":"update","job_id":"test-123"}'
TEST_DISTRIBUTIONS="alpine ubuntu debian rockylinux fedora arch opensuse"

# Go Test Configuration
GO_TEST_DISABLE_MONITORING=1